export enum TournamentStatus {
    Cancelled,
    Upcoming,
    InProgress,
    Completed,
}

export enum LeaderboardEntryStatus {
    Unknown,
    Cut,
    Withdrawn,
}

export enum EventType {
    Stroke,
    Cup,
    Match,
    Team,
}
